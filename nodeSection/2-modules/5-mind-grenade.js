const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`The sum is: ${num1 + num2}`)
}

addValues()

// When you import a module you actualy invoke it.
// When node export module, it wraps all in function.